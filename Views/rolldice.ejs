<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>rolldice</title>
</head>
<body>
    <h2>Dice gave number: <%= diceValue = Math.floor(Math.random()*6) + 1 %></h2>
    <% if (diceValue == 6) { %>
        <h2>Nice! Roll dice again</h2>
    <% } %>
</body>
</html>